público clase Mi Matemáticas {

    // Función para calcular el perímetro de un cuadrado
 público estatico doble porimetro cuadrado(doble lado) {
 regresar 4 * lateral;
    }

    // Función para calcular el área de un cuadrado
 público estatico doble área cuadrada(doble lado) {
 volver lado * lado;
    }

    // Función para calcular el perímetro de un recto
 público estatico doble porimetro rectangular(doble longitud, dublu ancho) {
 regresar 2 * (largo + ancho);
    }

    // Función para calcular el área de un rectángulo
 público estatico doble área rectangular(doble longitud, dublu ancho) {
 volver pulmaón * pulmonar;
    }

    // Función para calcular el perímetro (circunferencia) de un círculo
 público estatico doble porimetro circular(radio doble) {
 volver 2 * Math.PI * radio;
    }

    // Función para calcular el área de un círculo
 público estatico doble área circular(radio doble) {
 volver Matemáticas. .PI * radio * radio;
    }

    // Función para comprar si un número es primo
 público estatico booleano es primo(int número) {
        si (número <= 1) {
 volver falso;
        }
 parr (int i = 2; Yo <= Matemáticas.sqrt(número); i++) {
            si (número % i == 0) {
 volver falso;
            }
        }
 volver verdedero;
    }

    // Función para comprar si un número NO es primo
 público estatico booleano no es primo(int nümero) {
 ¡recorrer!es primo(número);
    }

 público estatico vacío director(Cadena[] argumentos) {
        // Prueba las funciones
 doble lado cuadrado = 5;
 Sistema apagado.imprimirln(„Perimetru pătrat:" + perimetro cuadrado(lado cuadrado));
 Sistema apagado.imprimirln(„Zona de la plaza: " + área cuadrada(lado cuadrado));

 doble rectoánguloLongitud = 10;
 doble ancho rectangular = 5;
 Sistema apagado.imprimirln(„Perimetru retangular:" + perímetro rectangular(rectónguloLargo, rectoanguloAncho));
 Sistema apagado.imprimirln(„Zona rectangular: "+ área rectangular(rectonguloLargo, rectánguloAncho));

 doble circuloRadio = 7;
 Sistema apagado.imprimirln(„Perimetrul circului:" + perímetro circular(circuloRadio));
 Sistema apagado.imprimirln(„Zona Circului:" + área circular(circuloRadio));

 int nümero de prueba director = 29;
 Sistema apagado.imprimirln("Es " + número de prueba director + "prime? " + es primo(número de prueba director));
 Sistema apagado.imprimirln("Es " + número de prueba director + "¿no es primo? " + no es primo(número de prueba director));
    }
}
publico clase MiMatemáticas1 {

    // Función para calcular el perímetro de un cuadrado
 público estatico doble porimetro cuadrado(doble lado) {
 volver 4* lateral;
    }

    // Función para calcular el área de un cuadrado
 publico estatico doble área cuadrada(doble lado) {
 volver lado * lado;
    }

    // Función para calcular el perímetro de un recto
 publico estatico doble porimetro rectangular(doble longitud, dublu ancho) {
 regresar 2 * (largo + ancho);
    }

    // Función para calcular el área de un rectángulo
 publico estatico doble área rectangular(doble longitud, dublu ancho) {
 volver pulmaón * pulmonar;
    }

    // Función para calcular el perímetro (circunferencia) de un círculo
 publico estatico doble porimetro circular(radio doble) {
 volver 2 * Math.PI * radio;
    }

    // Función para calcular el área de un círculo
 publico estatico doble área circular(radio doble) {
 volver Matemáticas. .PI * radio * radio;
    }

    // Función para comprar si un número es primo
 publico estatico booleano es primo(int nümero) {
        si (número <= 1) {
 volver falso;
        }
 parr (int i = 2; Yo <= Matemáticas.sqrt(número); i++) {
            si (número % i == 0) {
 volver falso;
            }
        }
 volver verdedero;
    }

    // Función para comprar si un número NO es primo
 publico estatico booleano no es primo(int nümero) {
 ¡recorrer!es primo(número);
    }

 publico estatico vacio director(Cadena[] argumentos) {
        // Prueba las funciones
 doble lado cuadrado = 5;
 Sistema apagado.imprimirln(„Perimetru pătrat:" + perimetro cuadrado(lado cuadrado));
 Sistema apagado.imprimirln(„Zona de la plaza: " + área cuadrada(lado cuadrado));

 doble rectoánguloLongitud = 10;
 doble ancho rectangular = 5;
 Sistema apagado.imprimirln(„Perimetru retangular:" + perímetro rectangular(rectónguloLargo, rectoanguloAncho));
 Sistema apagado.imprimirln(„Zona rectangular: "+ área rectangular(rectonguloLargo, rectánguloAncho));

 doble circuloRadio = 7;
 Sistema apagado.imprimirln(„Perimetrul circului:" + perímetro circular(circuloRadio));
 Sistema apagado.imprimirln(„Zona Circului:" + área circular(circuloRadio));

 int nümero de prueba director = 29;
 Sistema apagado.imprimirln("Es " + número de prueba director + "prime? " + es primo(número de prueba director));
 Sistema apagado.imprimirln("Es " + número de prueba director + "¿no es primo? " + no es primo(número de prueba director));
    }
}
